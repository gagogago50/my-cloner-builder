name: Secure Build Cheat Engine

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      repository:
        description: "owner/repo (ex: cheat-engine/cheat-engine)"
        required: true
        default: "cheat-engine/cheat-engine"
        type: string
      ref:
        description: "branch / tag / sha"
        required: false
        default: "master"
        type: string
      artifact_path:
        description: "Glob du binaire a uploader"
        required: true
        default: "Cheat Engine/bin/**"
        type: string
      lazarus_version:
        description: "Version de Lazarus (2.2.2 recommande pour CE)"
        required: false
        default: "2.2.6"
        type: choice
        options:
          - "2.2.6"
          - "2.2.2"
      dotnet_version:
        description: "Version du SDK .NET pour DotNetDataCollector"
        required: false
        default: "8.0.x"
        type: choice
        options:
          - "8.0.x"
          - "9.0.x"
          - "7.0.x"
      enable_avx:
        description: "Activer les optimisations AVX/AVX2 pour DotNetDataCollector"
        required: false
        default: true
        type: boolean
      apply_code_patches:
        description: "Appliquer les patches d'amelioration du code Cheat Engine"
        required: false
        default: true
        type: boolean

permissions:
  contents: read
  security-events: write

jobs:
  security-scans:
    name: "Sequential Security Scans"
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.ref }}
          fetch-depth: 0

      - name: Create Gitleaks allowlist
        run: |
          cat > .gitleaks.toml << 'EOF'
          [extend]
          useDefault = true
          [allowlist]
          description = "Faux positifs connus - code source Cheat Engine tiers"
          paths = [
            "Cheat Engine/feces\.pas",
            "Cheat Engine/frmluaengineunit\.lfm"
          ]
          EOF

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
          GITLEAKS_CONFIG: ${{ github.workspace }}/.gitleaks.toml

      - name: Setup ClamAV
        continue-on-error: true
        run: |
          sudo apt-get update
          sudo apt-get install -y clamav clamav-daemon
          sudo freshclam --quiet || true

      - name: Scan repository with ClamAV
        continue-on-error: true
        run: clamscan --recursive --infected --quiet . || true

      - name: Pattern scan
        continue-on-error: true
        run: |
          grep -rniE "(password|secret|token|api_key)" --include="*.pas" --include="*.lfm" --include="*.lpi" --include="*.lpr" . || true

  build:
    name: "Build Windows AMD64 + x86 + AVX"
    runs-on: windows-latest
    needs: security-scans
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.ref }}
          submodules: recursive

      # ----------------------------------------------------------------------
      # Setup MSBuild + .NET SDK
      # ----------------------------------------------------------------------
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
        with:
          msbuild-architecture: x64

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.dotnet_version }}
        continue-on-error: true

      - name: Setup NuGet cache
        if: ${{ success() }}
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ inputs.dotnet_version }}-${{ hashFiles('**/DotNetDataCollector/**/*.csproj', '**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      # ----------------------------------------------------------------------
      # Setup Lazarus
      # ----------------------------------------------------------------------
      - name: Setup Lazarus
        uses: gcarreno/setup-lazarus@v3
        with:
          lazarus-version: ${{ inputs.lazarus_version }}
          with-cache: true

      - name: Resolve Lazarus directory
        shell: pwsh
        run: |
          $lazbuild = Get-Command lazbuild -ErrorAction SilentlyContinue
          $lazDir = $null
          if ($lazbuild) {
              $lazDir = Split-Path -Parent $lazbuild.Source
              while ($lazDir -and -not (Test-Path (Join-Path $lazDir "fpc"))) {
                  $lazDir = Split-Path -Parent $lazDir
              }
          }
          $candidates = @("D:\a\_temp\installers\lazarus", "C:\lazarus", "C:\Program Files\Lazarus")
          if (-not $lazDir) {
              foreach ($c in $candidates) {
                  if (Test-Path (Join-Path $c "lazbuild.exe")) { $lazDir = $c; break }
              }
          }
          if (-not $lazDir) {
              $found = Get-ChildItem -Path "D:\a" -Filter "lazbuild.exe" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($found) { $lazDir = Split-Path -Parent $found.FullName }
          }
          if (-not $lazDir) { throw "Dossier Lazarus introuvable" }
          Write-Host "Racine Lazarus: $lazDir"
          echo "LAZARUS_DIR=$lazDir" >> $env:GITHUB_ENV

      # ----------------------------------------------------------------------
      # Cross-compiler i386-win32 (best-effort)
      # ----------------------------------------------------------------------
      - name: Try install i386 cross-compiler (non-fatal, with retry)
        continue-on-error: true
        shell: pwsh
        env:
          LAZARUS_DIR: ${{ env.LAZARUS_DIR }}
        run: |
          $lazDir = $env:LAZARUS_DIR
          $fpcVer = "3.2.2"
          $lazVer = "${{ inputs.lazarus_version }}"
          try {
              $lazVerRaw = & lazbuild --version 2>&1 | Select-String -Pattern "Lazarus\s+(\d+\.\d+\.\d+)" | Select-Object -First 1
              if ($lazVerRaw -match "(\d+\.\d+\.\d+)") { $lazVer = $matches[1] }
          } catch {}
          Write-Host "Version Lazarus: $lazVer"

          $crossInstaller = "lazarus-$lazVer-fpc-$fpcVer-cross-i386-win32-win64.exe"
          $dest = Join-Path $env:RUNNER_TEMP $crossInstaller

          $mirrors = @(
              "https://downloads.sourceforge.net/project/lazarus/Lazarus%20Windows%2064%20bits/Lazarus%20$lazVer/$crossInstaller",
              "https://sourceforge.net/projects/lazarus/files/Lazarus%20Windows%2064%20bits/Lazarus%20$lazVer/$crossInstaller/download",
              "https://freepascal.dfmk.hu/pub/lazarus/releases/$crossInstaller"
          )

          function Download-WithRetry {
              param([string[]]$Urls, [string]$OutFile, [int]$MaxAttempts = 3)
              foreach ($url in $Urls) {
                  for ($i = 1; $i -le $MaxAttempts; $i++) {
                      Write-Host "Tentative $i/$MaxAttempts : $url"
                      try {
                          $headers = @{ "Accept" = "*/*" }
                          Invoke-WebRequest -Uri $url -OutFile $OutFile -UseBasicParsing `
                              -UserAgent "Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:128.0) Gecko/20100101 Firefox/128.0" `
                              -Headers $headers `
                              -MaximumRedirection 15 -TimeoutSec 120 -ErrorAction Stop
                          if (Test-Path $OutFile) {
                              $bytes = [System.IO.File]::ReadAllBytes($OutFile)[0..1]
                              $magic = [System.Text.Encoding]::ASCII.GetString($bytes)
                              if ($magic -eq "MZ") {
                                  Write-Host "Telecharge ($((Get-Item $OutFile).Length) bytes) depuis $url"
                                  return $true
                              } else {
                                  Write-Warning "Pas un .exe (magic='$magic')."
                                  Remove-Item $OutFile -Force -ErrorAction SilentlyContinue
                              }
                          }
                      } catch {
                          Write-Warning "Echec: $($_.Exception.Message)"
                          Start-Sleep -Seconds ([Math]::Min($i * 5, 15))
                      }
                  }
              }
              return $false
          }

          $downloaded = Download-WithRetry -Urls $mirrors -OutFile $dest -MaxAttempts 3

          if (-not $downloaded) {
              Write-Warning "Telechargement cross-compiler impossible. Le mode 32-bit sera ignore."
              return
          }

          Write-Host "::group::Installation cross-compiler i386"
          $logFile = Join-Path $env:RUNNER_TEMP "laz_cross_install.log"
          $p = Start-Process -FilePath "$dest" `
            -ArgumentList "/verysilent /suppressmsgboxes /norestart /nocloseapplications /DIR=`"$lazDir`" /LOG=`"$logFile`"" `
            -Wait -PassThru -NoNewWindow
          Write-Host "Code sortie install: $($p.ExitCode)"
          Write-Host "::endgroup::"

          $ppc386 = Get-ChildItem -Path $lazDir -Filter "ppc386.exe" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
          if ($ppc386) {
              Write-Host "ppc386.exe OK: $($ppc386.FullName)"
              $fpcBinDir = Split-Path -Parent $ppc386.FullName
              echo "$fpcBinDir" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
              $env:PATH = "$fpcBinDir;$env:PATH"
              echo "HAVE_PPC386=true" >> $env:GITHUB_ENV
          } else {
              Write-Warning "ppc386.exe introuvable apres installation."
          }

      # ----------------------------------------------------------------------
      # Patches d'amelioration du code
      # ----------------------------------------------------------------------
      - name: Apply Cheat Engine code improvement patches
        if: ${{ inputs.apply_code_patches == true }}
        shell: pwsh
        run: |
          $ceDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine"
          $patchLog = Join-Path $env:GITHUB_WORKSPACE "patches_applied.md"
          "# Patches d'amelioration appliques" | Out-File -FilePath $patchLog -Encoding utf8

          function Add-PatchLog {
              param([string]$Msg)
              Add-Content -Path $patchLog -Value "- $Msg"
              Write-Host "PATCH: $Msg"
          }

          # --- Patch 1: DotNetDataCollector - Ajout support AVX2 intrinsiques ---
          $dncDir = Join-Path $ceDir "DotNetDataCollector"
          if (Test-Path $dncDir) {
              $avxHelperPath = Join-Path $dncDir "Avx2MemoryScanner.cs"
              if (-not (Test-Path $avxHelperPath)) {
                  $avxCode = @'
          // Avx2MemoryScanner.cs - Amelioration: scanner memoire vectorise AVX2
          using System;
          using System.Runtime.Intrinsics;
          using System.Runtime.Intrinsics.X86;

          namespace DotNetDataCollector
          {
              internal static class Avx2MemoryScanner
              {
                  public static bool IsSupported => Avx2.IsSupported;

                  public static unsafe bool ContainsPattern(byte* buffer, int length, byte pattern, byte mask)
                  {
                      if (length <= 0) return false;
                      if (Avx2.IsSupported && length >= 32) {
                          var vPattern = Vector256.Create(pattern);
                          var vMask    = Vector256.Create(mask);
                          int i = 0;
                          int limit = length - 32;
                          while (i <= limit) {
                              var v = Avx.LoadVector256(buffer + i);
                              var masked = Avx2.And(v, vMask);
                              var cmp = Avx2.CompareEqual(masked, vPattern);
                              if (cmp.GetElement(0) != 0 || cmp.GetElement(1) != 0 ||
                                  cmp.GetElement(2) != 0 || cmp.GetElement(3) != 0) {
                                  return true;
                              }
                              i += 32;
                          }
                          for (; i < length; i++) {
                              if ((buffer[i] & mask) == (pattern & mask)) return true;
                          }
                          return false;
                      }
                      if (Sse2.IsSupported && length >= 16) {
                          var vPattern = Vector128.Create(pattern);
                          var vMask    = Vector128.Create(mask);
                          int i = 0;
                          int limit = length - 16;
                          while (i <= limit) {
                              var v = Sse2.LoadVector128(buffer + i);
                              var masked = Sse2.And(v, vMask);
                              var cmp = Sse2.CompareEqual(masked, vPattern);
                              if (cmp.GetElement(0) != 0 || cmp.GetElement(1) != 0) return true;
                              i += 16;
                          }
                          for (; i < length; i++) {
                              if ((buffer[i] & mask) == (pattern & mask)) return true;
                          }
                          return false;
                      }
                      for (int i = 0; i < length; i++) {
                          if ((buffer[i] & mask) == (pattern & mask)) return true;
                      }
                      return false;
                  }

                  public static unsafe int IndexOfPattern(byte* buffer, int length, byte[] pattern, byte[] mask)
                  {
                      if (pattern == null || pattern.Length == 0 || length < pattern.Length) return -1;
                      int last = length - pattern.Length;
                      for (int i = 0; i <= last; i++) {
                          int j = 0;
                          while (j < pattern.Length && (buffer[i + j] & mask[j]) == (pattern[j] & mask[j])) j++;
                          if (j == pattern.Length) return i;
                      }
                      return -1;
                  }
              }
          }
          '@
                  $avxCode | Out-File -FilePath $avxHelperPath -Encoding utf8
                  Add-PatchLog "Ajout Avx2MemoryScanner.cs (support AVX2/SSE2 avec fallback scalaire)"
              }

              $csprojFiles = Get-ChildItem -Path $dncDir -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue
              foreach ($csproj in $csprojFiles) {
                  $content = Get-Content $csproj.FullName -Raw -Encoding UTF8
                  $modified = $false
                  if ($content -notmatch '<Optimize>') {
                      $content = $content -replace '</PropertyGroup>', '  <Optimize>true</Optimize>`n  <TieredCompilation>true</TieredCompilation>`n  <ServerGarbageCollection>true</ServerGarbageCollection>`n  <ConcurrentGarbageCollection>true</ConcurrentGarbageCollection>`n  <DisableFastUpToDateCheck>true</DisableFastUpToDateCheck>`n</PropertyGroup>'
                      $modified = $true
                      Add-PatchLog "$($csproj.Name): active Optimize+TieredCompilation+ServerGC"
                  }
                  if ($content -notmatch '<EnableHWIntrinsic') {
                      $content = $content -replace '</Project>', '  <PropertyGroup Condition="''$(Configuration)''==''Release''">`n    <EnableHWIntrinsic>true</EnableHWIntrinsic>`n    <AVX>true</AVX>`n  </PropertyGroup>`n</Project>'
                      $modified = $true
                      Add-PatchLog "$($csproj.Name): active EnableHWIntrinsic+AVX en Release"
                  }
                  if ($modified) {
                      $content = $content -replace '`n', "`n"
                      Set-Content -Path $csproj.FullName -Value $content -Encoding UTF8 -NoNewline
                  }
              }

              $csFiles = Get-ChildItem -Path $dncDir -Filter "*.cs" -Recurse -ErrorAction SilentlyContinue |
                  Where-Object { $_.Name -notmatch 'Avx2MemoryScanner' }
              foreach ($cs in $csFiles) {
                  try {
                      $content = Get-Content $cs.FullName -Raw -Encoding UTF8
                      $orig = $content
                      $content = [System.Text.RegularExpressions.Regex]::Replace(
                          $content,
                          'catch\s*\(\s*Exception(\s+\w+)?\s*\)\s*\{\s*\}',
                          'catch (System.Exception ex$1) { System.Diagnostics.Debug.WriteLine("DotNetDataCollector: " + ex.ToString()); }'
                      )
                      $content = [System.Text.RegularExpressions.Regex]::Replace(
                          $content,
                          'catch\s*\{\s*\}',
                          'catch (System.Exception ex) { System.Diagnostics.Debug.WriteLine("DotNetDataCollector: " + ex.ToString()); }'
                      )
                      if ($content -ne $orig) {
                          Set-Content -Path $cs.FullName -Value $content -Encoding UTF8 -NoNewline
                          Add-PatchLog "$($cs.Name): remplace les catch vides par logging debug"
                      }
                  } catch {
                      Write-Warning "Patch exception handling echoue sur $($cs.Name): $_"
                  }
              }
          } else {
              Add-PatchLog "DotNetDataCollector absent - patches .NET sautes"
          }

          # --- Patch 4: lazbuild - activer -O4 AVX2 ---
          $lpiFiles = Get-ChildItem -Path $ceDir -Filter "*.lpi" -Recurse -ErrorAction SilentlyContinue
          $avxModesAdded = 0
          foreach ($lpi in $lpiFiles) {
              try {
                  $content = Get-Content $lpi.FullName -Raw -Encoding UTF8
                  if ($content -match 'O4 AVX2') { continue }
                  if ($content -match '<Mode Name="Release 64-Bit">') {
                      $newMode = '    <Mode Name="Release 64-Bit O4 AVX2">' + "`n" +
                                  '      <CompilerOptions>`n' +
                                  '        <Options>`n' +
                                  '          <Optimization Value="4"/>`n' +
                                  '          <CpuType Value="AVX2"/>`n' +
                                  '        </Options>`n' +
                                  '      </CompilerOptions>`n' +
                                  '    </Mode>'
                      if ($lpi.Name -eq 'cheatengine.lpi') {
                          $content = $content -replace '(<BuildModes>)', "`$1`n$newMode"
                          Set-Content -Path $lpi.FullName -Value $content -Encoding UTF8 -NoNewline
                          $avxModesAdded++
                      }
                  }
              } catch {
                  Write-Warning "Patch AVX2 sur $($lpi.Name) echoue: $_"
              }
          }
          if ($avxModesAdded -gt 0) {
              Add-PatchLog "Mode 'Release 64-Bit O4 AVX2' ajoute a $avxModesAdded projet(s) Pascal"
          }

          Write-Host "::group::Resume des patches appliques"
          Get-Content $patchLog | ForEach-Object { Write-Host $_ }
          Write-Host "::endgroup::"

      # ----------------------------------------------------------------------
      # Enregistrer les packages personnalises CE
      # ----------------------------------------------------------------------
      - name: Register and Build Cheat Engine Packages
        shell: pwsh
        env:
          LAZARUS_DIR: ${{ env.LAZARUS_DIR }}
        run: |
          $ceDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine"
          $packages = Get-ChildItem -Path $ceDir -Filter "*.lpk" -Recurse | Sort-Object Name
          Write-Host "Packages trouves: $($packages.Count)"
          foreach ($pkg in $packages) {
              Write-Host "::group::Ajout: $($pkg.Name)"
              $ok = $false
              for ($i = 1; $i -le 2; $i++) {
                  try {
                      & lazbuild --add-package "$($pkg.FullName)" 2>&1
                      if ($LASTEXITCODE -eq 0) { $ok = $true; break }
                      Write-Warning "Erreur ajout $($pkg.Name) (tentative $i)"
                  } catch { Write-Warning "Exception ajout $($pkg.Name) (tentative $i): $_" }
                  Start-Sleep -Seconds 2
              }
              if (-not $ok) { Write-Warning "Echec definitif ajout $($pkg.Name)" }
              Write-Host "::endgroup::"
          }
          & lazbuild --build-ide= 2>&1 | Out-Null

      # ----------------------------------------------------------------------
      # Build Cheat Engine principal
      # ----------------------------------------------------------------------
      - name: Build Cheat Engine (all release modes)
        shell: pwsh
        run: |
          $workspace = $env:GITHUB_WORKSPACE
          $projectPath = Join-Path $workspace "Cheat Engine\cheatengine.lpi"
          if (-not (Test-Path $projectPath)) { throw "Projet non trouve: $projectPath" }

          $criticalModes = @("Release 64-Bit", "Release 64-Bit O4 AVX2")
          $optionalModes = @()
          if ($env:HAVE_PPC386 -eq "true") { $optionalModes += "Release 32-Bit" }

          foreach ($mode in $criticalModes) {
              Write-Host "::group::Compilation CRITIQUE: $mode"
              & lazbuild --build-mode="$mode" --max-process-count=4 "$projectPath" 2>&1
              $exitCode = $LASTEXITCODE
              Write-Host "::endgroup::"
              if ($exitCode -ne 0) {
                  if ($mode -like "*AVX2*") {
                      Write-Warning "Mode CRITIQUE '$mode' echoue (code $exitCode) - degrade en non-fatal"
                  } else {
                      throw "Mode CRITIQUE '$mode' echoue (code $exitCode)"
                  }
              }
          }
          foreach ($mode in $optionalModes) {
              Write-Host "::group::Compilation OPTIONNEL: $mode"
              & lazbuild --build-mode="$mode" --max-process-count=4 "$projectPath" 2>&1
              $exitCode = $LASTEXITCODE
              Write-Host "::endgroup::"
              if ($exitCode -ne 0) { Write-Warning "Mode OPTIONNEL '$mode' echoue - ignore." }
          }

          Write-Host "::group::Binaires generes"
          $binDir = Join-Path $workspace "Cheat Engine\bin"
          if (Test-Path $binDir) {
              Get-ChildItem -Path $binDir -Filter "*.exe" | ForEach-Object {
                  Write-Host "Binaire: $($_.Name) ($($_.Length) bytes)"
              }
          }
          Write-Host "::endgroup::"
          exit 0

      # ----------------------------------------------------------------------
      # Build DotNetDataCollector — x64
      # ----------------------------------------------------------------------
      - name: Build DotNetDataCollector (Windows AMD64 / x64)
        shell: pwsh
        run: |
          $dncDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\DotNetDataCollector"
          if (-not (Test-Path $dncDir)) { Write-Warning "DotNetDataCollector absent"; exit 0 }

          $dotnet = Get-Command dotnet -ErrorAction SilentlyContinue
          if (-not $dotnet) {
              $sln = Get-ChildItem -Path $dncDir -Filter "*.sln" -ErrorAction SilentlyContinue | Select-Object -First 1
              $csproj = Get-ChildItem -Path $dncDir -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($sln) { & msbuild $sln.FullName /nologo /verbosity:minimal /p:Configuration=Release /p:Platform=x64 2>&1 }
              elseif ($csproj) { & msbuild $csproj.FullName /nologo /verbosity:minimal /p:Configuration=Release /p:Platform=x64 2>&1 }
              exit 0
          }

          $proj = Get-ChildItem -Path $dncDir -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $proj) { Write-Warning "Aucun .csproj"; exit 0 }

          Write-Host "::group::dotnet build x64 (Release)"
          & dotnet build $proj.FullName -c Release -r win-x64 --self-contained false -p:Platform=x64 2>&1
          if ($LASTEXITCODE -ne 0) {
              & dotnet build $proj.FullName -c Release -p:Platform=x64 2>&1
          }
          Write-Host "::endgroup::"

          $binDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\bin"
          New-Item -ItemType Directory -Path $binDir -Force | Out-Null
          $outDirs = @((Join-Path $dncDir "bin\Release"), (Join-Path $dncDir "bin\x64\Release"), (Join-Path $dncDir "obj\Release"))
          foreach ($d in $outDirs) {
              if (Test-Path $d) {
                  Get-ChildItem -Path $d -Recurse -Include "DotNetDataCollector*.dll","DotNetDataCollector*.exe" -ErrorAction SilentlyContinue | ForEach-Object {
                      Copy-Item $_.FullName -Destination $binDir -Force -ErrorAction SilentlyContinue
                      Write-Host "Copie (x64): $($_.Name)"
                  }
              }
          }
          exit 0

      # ----------------------------------------------------------------------
      # Build DotNetDataCollector — x86
      # ----------------------------------------------------------------------
      - name: Build DotNetDataCollector (Windows x86 / 32-bit)
        shell: pwsh
        run: |
          $dncDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\DotNetDataCollector"
          if (-not (Test-Path $dncDir)) { Write-Warning "DotNetDataCollector absent"; exit 0 }

          $dotnet = Get-Command dotnet -ErrorAction SilentlyContinue
          if (-not $dotnet) {
              $sln = Get-ChildItem -Path $dncDir -Filter "*.sln" -ErrorAction SilentlyContinue | Select-Object -First 1
              $csproj = Get-ChildItem -Path $dncDir -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($sln) { & msbuild $sln.FullName /nologo /verbosity:minimal /p:Configuration=Release /p:Platform=x86 2>&1 }
              elseif ($csproj) { & msbuild $csproj.FullName /nologo /verbosity:minimal /p:Configuration=Release /p:Platform=x86 2>&1 }
              exit 0
          }

          $proj = Get-ChildItem -Path $dncDir -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $proj) { Write-Warning "Aucun .csproj"; exit 0 }

          Write-Host "::group::dotnet build x86 (Release)"
          & dotnet build $proj.FullName -c Release -r win-x86 --self-contained false -p:Platform=x86 2>&1
          if ($LASTEXITCODE -ne 0) {
              & dotnet build $proj.FullName -c Release -p:Platform=x86 2>&1
          }
          Write-Host "::endgroup::"

          $binDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\bin"
          $binX86 = Join-Path $binDir "x86"
          New-Item -ItemType Directory -Path $binX86 -Force | Out-Null
          $outDirs = @((Join-Path $dncDir "bin\Release"), (Join-Path $dncDir "bin\x86\Release"), (Join-Path $dncDir "obj\Release"))
          foreach ($d in $outDirs) {
              if (Test-Path $d) {
                  Get-ChildItem -Path $d -Recurse -Include "DotNetDataCollector*.dll","DotNetDataCollector*.exe" -ErrorAction SilentlyContinue | ForEach-Object {
                      Copy-Item $_.FullName -Destination $binX86 -Force -ErrorAction SilentlyContinue
                      Write-Host "Copie (x86): $($_.Name)"
                  }
              }
          }
          exit 0

      # ----------------------------------------------------------------------
      # Build DotNetDataCollector — variante AVX/AVX2
      # ----------------------------------------------------------------------
      - name: Build DotNetDataCollector (AVX / AVX2 variant)
        if: ${{ inputs.enable_avx == true }}
        shell: pwsh
        run: |
          $dncDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\DotNetDataCollector"
          if (-not (Test-Path $dncDir)) { Write-Warning "Absent"; exit 0 }

          $dotnet = Get-Command dotnet -ErrorAction SilentlyContinue
          if (-not $dotnet) { Write-Warning "SDK .NET indisponible"; exit 0 }

          $proj = Get-ChildItem -Path $dncDir -Filter "*.csproj" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $proj) { Write-Warning "Aucun .csproj"; exit 0 }

          Write-Host "::group::dotnet build AVX (Release, x64)"
          & dotnet build $proj.FullName -c Release -r win-x64 --self-contained false `
              -p:Platform=x64 -p:DefineConstants="AVX;AVX2" -p:Optimize=true -p:TieredCompilation=true -p:AVX=true 2>&1
          if ($LASTEXITCODE -ne 0) { Write-Warning "dotnet build AVX echoue"; exit 0 }
          Write-Host "::endgroup::"

          $binDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\bin"
          $binAvx = Join-Path $binDir "avx"
          New-Item -ItemType Directory -Path $binAvx -Force | Out-Null
          $outDirs = @((Join-Path $dncDir "bin\Release"), (Join-Path $dncDir "bin\x64\Release"), (Join-Path $dncDir "obj\Release"))
          foreach ($d in $outDirs) {
              if (Test-Path $d) {
                  Get-ChildItem -Path $d -Recurse -Include "DotNetDataCollector*.dll","DotNetDataCollector*.exe" -ErrorAction SilentlyContinue | ForEach-Object {
                      $destName = $_.BaseName + ".avx" + $_.Extension
                      Copy-Item $_.FullName -Destination (Join-Path $binAvx $destName) -Force -ErrorAction SilentlyContinue
                      Write-Host "Copie (AVX): $destName"
                  }
              }
          }
          exit 0

      # ----------------------------------------------------------------------
      # Build Lazarus sub-projects (speedhack, luaclient, VEHDebug, etc.)
      # ----------------------------------------------------------------------
      - name: Build Lazarus sub-projects
        shell: pwsh
        run: |
          $ceDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine"
          $binDir = Join-Path $ceDir "bin"

          $subDirs = @(
              "speedhack", "luaclient", "VEHDebug", "Tutorial", "betterControls",
              "plugin", "launcher", "winhook", "xmplayer"
          )
          $skipDirs = @("windowsrepair", "sfx", "ceregreset", "common", "release")

          foreach ($sub in $subDirs) {
              $p = Join-Path $ceDir $sub
              if (-not (Test-Path $p)) { continue }

              $lpiFiles = Get-ChildItem -Path $p -Filter "*.lpi" -Recurse -ErrorAction SilentlyContinue
              foreach ($lpi in $lpiFiles) {
                  Write-Host "::group::lazbuild $($lpi.Name) [64-bit]"
                  & lazbuild --build-mode="Release" --cpu=x86_64 --os=win64 --ws=win32 --max-process-count=4 "$($lpi.FullName)" 2>&1
                  if ($LASTEXITCODE -ne 0) { Write-Warning "Echec $($lpi.Name) [Release 64-bit]" }
                  Write-Host "::endgroup::"
              }

              $lprFiles = Get-ChildItem -Path $p -Filter "*.lpr" -Recurse -ErrorAction SilentlyContinue
              foreach ($lpr in $lprFiles) {
                  Write-Host "::group::lazbuild $($lpr.Name) [64-bit]"
                  & lazbuild --cpu=x86_64 --os=win64 --ws=win32 --max-process-count=4 "$($lpr.FullName)" 2>&1
                  if ($LASTEXITCODE -ne 0) { Write-Warning "Echec $($lpr.Name) [64-bit]" }
                  Write-Host "::endgroup::"
              }
          }

          if ($env:HAVE_PPC386 -eq "true") {
              Write-Host ">> Cross-compiler ppc386 present - compilation 32-bit"
              foreach ($sub in ($subDirs + $skipDirs)) {
                  $p = Join-Path $ceDir $sub
                  if (-not (Test-Path $p)) { continue }
                  $lpiFiles = Get-ChildItem -Path $p -Filter "*.lpi" -Recurse -ErrorAction SilentlyContinue
                  foreach ($lpi in $lpiFiles) {
                      Write-Host "::group::lazbuild $($lpi.Name) [32-bit]"
                      & lazbuild --build-mode="Release" --cpu=i386 --os=win32 --ws=win32 --max-process-count=4 "$($lpi.FullName)" 2>&1
                      if ($LASTEXITCODE -ne 0) { Write-Warning "Echec $($lpi.Name) [32-bit]" }
                      Write-Host "::endgroup::"
                  }
              }
          } else {
              Write-Host ">> ppc386 absent - compilation 32-bit skip"
          }
          exit 0

      # ----------------------------------------------------------------------
      # Restore NuGet pour toutes les solutions .NET (corrige l'erreur assets.json)
      # ----------------------------------------------------------------------
      - name: Restore NuGet packages (.NET solutions)
        shell: pwsh
        run: |
          $ceDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine"
          $dotnet = Get-Command dotnet -ErrorAction SilentlyContinue
          if (-not $dotnet) { Write-Warning "dotnet introuvable - skip restore"; exit 0 }

          $netSolutions = @("DotNetDataCollector.sln", "DotNetInvasiveDataCollector.sln", "MonoDataCollector.sln", "CEJVMTI.sln", "CSCompiler.sln")
          foreach ($solName in $netSolutions) {
              $solPath = Get-ChildItem -Path $ceDir -Filter $solName -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($solPath) {
                  Write-Host "::group::nuget restore $solName"
                  & dotnet restore "$($solPath.FullName)" 2>&1
                  if ($LASTEXITCODE -ne 0) { Write-Warning "Restore echoue pour $solName" }
                  Write-Host "::endgroup::"
              } else {
                  Write-Warning "$solName introuvable"
              }
          }
          exit 0

      # ----------------------------------------------------------------------
      # Build Visual Studio sub-projects (cible par solution, plateformes correctes)
      # ----------------------------------------------------------------------
      - name: Build Visual Studio sub-projects (.sln)
        shell: pwsh
        run: |
          $ceDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine"
          $binDir = Join-Path $ceDir "bin"
          New-Item -ItemType Directory -Path $binDir -Force | Out-Null

          # Definition des solutions avec leurs plateformes et criticite
          $solutions = @(
              @{ Name="DotNetDataCollector.sln";       Platforms=@("x86","x64"); IsNet=$true;  Critical=$false },
              @{ Name="DotNetInvasiveDataCollector.sln"; Platforms=@("Any CPU");   IsNet=$true;  Critical=$false },
              @{ Name="MonoDataCollector.sln";         Platforms=@("x86","x64"); IsNet=$true;  Critical=$false },
              @{ Name="CEJVMTI.sln";                   Platforms=@("x86","x64"); IsNet=$true;  Critical=$false },
              @{ Name="Direct x mess.sln";             Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="CSCompiler.sln";                Platforms=@("Any CPU");   IsNet=$true;  Critical=$false },
              @{ Name="tcc.sln";                       Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="dbkkernel.sln";                 Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="cudaPointerScan.sln";           Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="lua53.sln";                     Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="CEPluginLibrary.sln";           Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="example-c.sln";                 Platforms=@("x86","x64"); IsNet=$false; Critical=$false },
              @{ Name="QWS.sln";                       Platforms=@("x86","x64"); IsNet=$false; Critical=$false }
          )

          foreach ($sol in $solutions) {
              $solPath = Get-ChildItem -Path $ceDir -Filter $sol.Name -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
              if (-not $solPath) {
                  Write-Warning "$($sol.Name) introuvable dans l'arbre - skip"
                  continue
              }

              Write-Host "::group::msbuild $($sol.Name)"

              foreach ($plat in $sol.Platforms) {
                  $msbuildPlat = $plat
                  if ($plat -eq "Any CPU") { $msbuildPlat = "AnyCPU" }

                  Write-Host "--- Plateforme: $plat (msbuild: $msbuildPlat) ---"
                  & msbuild "$($solPath.FullName)" `
                      /nologo /verbosity:minimal `
                      /p:Configuration=Release `
                      /p:Platform="$msbuildPlat" 2>&1

                  $exit = $LASTEXITCODE
                  if ($exit -ne 0) {
                      if ($sol.Critical) {
                          throw "BUILD CRITIQUE ECHOUE: $($sol.Name) [$plat] (code $exit)"
                      } else {
                          Write-Warning "BUILD NON-CRITIQUE ECHOUE: $($sol.Name) [$plat] (code $exit)"
                      }
                  }
              }
              Write-Host "::endgroup::"
          }
          exit 0

      # ----------------------------------------------------------------------
      # Copier tcclib DLLs
      # ----------------------------------------------------------------------
      - name: Copy tcclib DLLs to bin
        shell: pwsh
        run: |
          $tcclibOut = Get-ChildItem -Path (Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\tcclib") -Filter "*.dll" -Recurse -ErrorAction SilentlyContinue |
              Where-Object { $_.Name -match "tcclib" }
          if ($tcclibOut) {
              $binDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\bin"
              foreach ($dll in $tcclibOut) {
                  Copy-Item $dll.FullName -Destination $binDir -Force -ErrorAction SilentlyContinue
                  Write-Host "Copie $($dll.Name) -> bin"
              }
          } else { Write-Host "Aucune DLL tcclib trouvee." }
          exit 0

      # ----------------------------------------------------------------------
      # Checksums SHA256 + manifeste
      # ----------------------------------------------------------------------
      - name: Generate SHA256 checksums and build manifest
        shell: pwsh
        run: |
          $binDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\bin"
          if (-not (Test-Path $binDir)) { Write-Warning "bin introuvable"; exit 0 }

          $manifest = Join-Path $env:GITHUB_WORKSPACE "build_manifest.txt"
          "Cheat Engine Build Manifest" | Out-File -FilePath $manifest -Encoding utf8
          "Generated: $(Get-Date -Format 'o')" | Out-File -FilePath $manifest -Append -Encoding utf8
          "Repository: ${{ inputs.repository }}" | Out-File -FilePath $manifest -Append -Encoding utf8
          "Ref: ${{ inputs.ref }}" | Out-File -FilePath $manifest -Append -Encoding utf8
          "Lazarus: ${{ inputs.lazarus_version }}" | Out-File -FilePath $manifest -Append -Encoding utf8
          ".NET: ${{ inputs.dotnet_version }}" | Out-File -FilePath $manifest -Append -Encoding utf8
          "AVX enabled: ${{ inputs.enable_avx }}" | Out-File -FilePath $manifest -Append -Encoding utf8
          "Patches applied: ${{ inputs.apply_code_patches }}" | Out-File -FilePath $manifest -Append -Encoding utf8
          "" | Out-File -FilePath $manifest -Append -Encoding utf8
          "Files:" | Out-File -FilePath $manifest -Append -Encoding utf8

          $hashes = Join-Path $env:GITHUB_WORKSPACE "SHA256SUMS.txt"
          Get-ChildItem -Path $binDir -Recurse -File | Sort-Object FullName | ForEach-Object {
              $rel = $_.FullName.Substring($binDir.Length + 1).Replace('\','/')
              $sha = (Get-FileHash -Path $_.FullName -Algorithm SHA256).Hash.ToLower()
              "{0} *{1}" -f $sha, $rel | Out-File -FilePath $hashes -Append -Encoding utf8
              "  {0,-55} {1,12} bytes  sha256:{2}" -f $rel, $_.Length, $sha.Substring(0,12) | Out-File -FilePath $manifest -Append -Encoding utf8
          }
          Copy-Item $manifest -Destination $binDir -Force -ErrorAction SilentlyContinue
          Copy-Item $hashes -Destination $binDir -Force -ErrorAction SilentlyContinue
          Write-Host "Manifeste: $manifest"
          Write-Host "Checksums: $hashes"
          exit 0

      # ----------------------------------------------------------------------
      # Resume final + GitHub Step Summary
      # ----------------------------------------------------------------------
      - name: List final bin folder + write GitHub Step Summary
        shell: pwsh
        run: |
          $binDir = Join-Path $env:GITHUB_WORKSPACE "Cheat Engine\bin"
          $summary = $env:GITHUB_STEP_SUMMARY
          if (-not $summary) { $summary = Join-Path $env:GITHUB_WORKSPACE "summary.md" }

          "# Cheat Engine - Build Summary" | Out-File -FilePath $summary -Encoding utf8
          "" | Out-File -FilePath $summary -Append -Encoding utf8
          "- Repository: ``${{ inputs.repository }}``" | Out-File -FilePath $summary -Append -Encoding utf8
          "- Ref: ``${{ inputs.ref }}``" | Out-File -FilePath $summary -Append -Encoding utf8
          "- Lazarus: ``${{ inputs.lazarus_version }}``" | Out-File -FilePath $summary -Append -Encoding utf8
          "- .NET SDK: ``${{ inputs.dotnet_version }}``" | Out-File -FilePath $summary -Append -Encoding utf8
          "- AVX active: ``${{ inputs.enable_avx }}``" | Out-File -FilePath $summary -Append -Encoding utf8
          "- Patches appliques: ``${{ inputs.apply_code_patches }}``" | Out-File -FilePath $summary -Append -Encoding utf8
          "- ppc386 disponible: ``$($env:HAVE_PPC386 -eq 'true')``" | Out-File -FilePath $summary -Append -Encoding utf8
          "" | Out-File -FilePath $summary -Append -Encoding utf8
          "## Binaires generes" | Out-File -FilePath $summary -Append -Encoding utf8
          "" | Out-File -FilePath $summary -Append -Encoding utf8
          "| Fichier | Taille (bytes) |" | Out-File -FilePath $summary -Append -Encoding utf8
          "|---------|---------------|" | Out-File -FilePath $summary -Append -Encoding utf8

          Write-Host "::group::Contenu de $binDir"
          if (Test-Path $binDir) {
              $totalSize = 0
              $totalFiles = 0
              Get-ChildItem -Path $binDir -Recurse -File | Sort-Object FullName | ForEach-Object {
                  $rel = $_.FullName.Substring($binDir.Length + 1)
                  Write-Host ("  {0,-50} {1,12} bytes" -f $rel, $_.Length)
                  "| $($rel.Replace('\','/')) | $($_.Length) |" | Out-File -FilePath $summary -Append -Encoding utf8
                  $script:totalSize += $_.Length
                  $script:totalFiles++
              }
              "" | Out-File -FilePath $summary -Append -Encoding utf8
              "**Total:** $totalFiles fichiers, $totalSize bytes" | Out-File -FilePath $summary -Append -Encoding utf8
          } else {
              Write-Warning "bin introuvable!"
              "_bin introuvable_" | Out-File -FilePath $summary -Append -Encoding utf8
          }
          Write-Host "::endgroup::"

      - name: Upload artifact (main)
        uses: actions/upload-artifact@v4
        with:
          name: cheat-engine-win64-${{ inputs.ref || 'master' }}
          path: ${{ inputs.artifact_path }}
          if-no-files-found: error
          retention-days: 14

      - name: Upload DotNetDataCollector artifacts (x64, x86, AVX)
        uses: actions/upload-artifact@v4
        with:
          name: dotnetdatacollector-multiarch-${{ inputs.ref || 'master' }}
          path: |
            Cheat Engine/bin/DotNetDataCollector*
            Cheat Engine/bin/x86/DotNetDataCollector*
            Cheat Engine/bin/avx/DotNetDataCollector*
            Cheat Engine/bin/build_manifest.txt
            Cheat Engine/bin/SHA256SUMS.txt
          if-no-files-found: warn
          retention-days: 14

      - name: Upload patches log
        if: ${{ inputs.apply_code_patches == true }}
        uses: actions/upload-artifact@v4
        with:
          name: patches-applied-${{ inputs.ref || 'master' }}
          path: patches_applied.md
          if-no-files-found: warn
          retention-days: 14
